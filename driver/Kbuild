# Kbuild for the opt-in controller and HID-over-SPI modules.
ccflags-y := -I$(src)

# Keep the experimental controller distinct from the in-tree spi-amd module.
obj-m += sl4a-spi-amd.o
sl4a-spi-amd-objs := spi-amd.o

obj-m += sl4a-spi-hid.o
sl4a-spi-hid-objs := spi-hid-core.o spi-hid-capimg.o mshw0231-raw.o trace.o

# Optional companion switch: lets Mutter use its native tablet-mode path.
obj-m += sl4a-tablet-mode.o
